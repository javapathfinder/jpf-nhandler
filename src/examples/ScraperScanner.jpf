@using = jpf-nhandler

target = ScraperScanner

nhandler.delegateUnhandledNative = true

# To make this work with nhandler,
# 1. Add the line "public static native NumberFormat getNumberInstance(Locale inLocale);" to NumberFormat model class
# 2. Remove java.util.regex.Matcher and its native peer class from jpf-core
# 3. Remove java.util.regex.Pattern and its native peer class from jpf-core  